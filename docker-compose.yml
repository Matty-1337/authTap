services:
  authtap:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:${AUTHTAP_PORT:-3004}:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
